
[package]
authors = ["trifledmatter <hey@trifledmatter.com>"]
edition = { workspace = true }
license = { workspace = true }
repository = { workspace = true }
name = "nwep_node"
version = "0.1.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
quiche = { workspace = true, features = ["ffi"] }
napi = "3.6.1"
napi-derive = "3.4.0"
ring = { workspace = true }

[build-dependencies]
napi-build = "2.3.1"

[profile.release]
lto = true
strip = "symbols"

[features]
default = []
qlog = [] # TODO: address this, this is a simple placeholder for now to make the build pass